console.log('5' + 10);

// This code logs '510' instead of 15 because of implicit type coercion. First, the number 10 is converted into a string. Then, the string '5' and the string '10' are concatenated together to make the string '510'
